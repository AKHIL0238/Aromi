## Packages
framer-motion | Complex animations and page transitions
recharts | Data visualization for fitness stats
react-hook-form | Form state management
@hookform/resolvers | Zod integration for forms
clsx | Class name utility
tailwind-merge | Class name utility
date-fns | Date formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Chat uses SSE at /api/conversations/:id/messages
Auth uses /api/login and /api/logout
